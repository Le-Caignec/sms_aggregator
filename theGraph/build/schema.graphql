type ScretAdded @entity {
  id: Bytes!
  _from: newPerson! # Personn
  key: String! # string
  date: BigInt! # uint256
  description: String! # string
}

type newPerson @entity {
  id: Bytes!
  _from: Bytes! # address
  secrets: [ScretAdded!]! @derivedFrom(field: "_from")
}
